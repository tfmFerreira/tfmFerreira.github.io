/* styles.css */

/* Global styles */
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
}

header {
    background-color: #333;
    color: #fff;
    padding: 20px;
}

header h1 {
    margin: 0;
}

nav ul {
    list-style-type: none;
    padding: 0;
}

nav ul li {
    display: inline;
    margin-right: 20px;
}

nav ul li a {
    color: #fff;
    text-decoration: none;
}

main {
    padding: 20px;
}

section {
    margin-bottom: 10px;
}

footer {
    background-color: #333;
    color: #fff;
    text-align: center;
    padding: 10px 0;
}


.member-photo {
    height: 240px;
    width: auto; /* Adjust size as needed */
    object-fit: cover;
}

.logo {
    width: 440px; /* Adjust size as needed */
    height: auto;
    object-fit: cover;
    margin-top: 30px;
}

.member-text {
    max-width: 500px; /* Prevents text from stretching too wide */
}


.news-header {
    font-size: 28px; /* Make it larger */
    font-weight: bold;
    color: #004080; /* Dark blue for distinction */
    letter-spacing: 2px; /* Spaced-out letters for style */
    margin-bottom: 15px;
    border-bottom: 3px solid #004080; /* Optional underline */
    display: inline-block; /* Prevents full width underline */
    padding-bottom: 5px;
}

.news-entry {
    max-width: 600px;
    margin: 20px auto;
    padding: 10px;
    border-bottom: 1px solid #ccc;
}

.news-title {
    font-weight: bold;
    font-size: 18px;
    margin-bottom: 5px;
}

.section-heading {
    font-size: 24px;
    font-weight: bold;
    transition: color 0.3s, background-color 0.3s;
}

.active-section {
    color: white;
    background-color: #004080; /* Dark blue highlight */
    padding: 5px 10px;
    border-radius: 5px; /* Optional rounded effect */
}
/* Default styles - Side-by-side layout for desktop */
.members-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px; /* Adds spacing between members */
}

.member {
    display: flex;
    align-items: center;
    width: 80%; /* Controls width on larger screens */
    max-width: 600px; /* Limits max width for consistency */
    text-align: left; /* Align text to the left */
    gap: 20px; /* Space between image and text */
}

/* Style the image */
.member img {
    max-width: 350px; /* Adjusts image size */
    height: auto;
    border-radius: 10px; /* Optional: Adds rounded corners */
}

/* On smaller screens (mobile), move text below the image */
@media (max-width: 768px) {
    .member {
        flex-direction: column; /* Stack image and text */
        text-align: center; /* Center text */
    }

    .member img {
        max-width: 80%; /* Slightly larger image on mobile */
    }
}
